spring:
  profiles:
    active:
      - dev
=================================
spring:
  profiles: dev
  application:
    name: microservicecloud-config-com-dev
  http:
    encoding:
      charset: UTF-8
      enabled: true
      force: true
=================================
spring:
  profiles: test
  application:
    name: microservicecloud-config-com-test
  http:
    encoding:
      charset: UTF-8
      enabled: true
      force: true


